import React from 'react'
import Layout from './layout'

export default (props) => (
  <Layout>
    <div className='message__wrapper message--informative'>
      <h1>Dude, this page is out of scope too.</h1>
      <p>
        Sorry but the current pretense is not to implement the user contacts page.
      </p>
    </div>
  </Layout>
)